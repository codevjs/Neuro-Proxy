// Seed file is not needed anymore
// Superadmin role and user are automatically created during first registration
// See: repositories/database/(public)/auth/register/register.db.ts

console.log('No seeding needed - superadmin is created automatically on first registration');
